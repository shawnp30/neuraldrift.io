import Link from 'next/link';
import { pageMeta } from '@/lib/seo';
import { getGuides } from '@/lib/guides';
export const metadata = pageMeta('/guides', 'ComfyUI setup & troubleshooting guides', 'Browse installation, model setup, hardware and workflow troubleshooting guides for ComfyUI.');
export default function GuidesPage() { const guides = getGuides(); return <div className="nd-site"><div className="nd-shell nd-page"><p className="nd-eyebrow">LEARN & TROUBLESHOOT</p><h1>Make your next run easier.</h1><p className="nd-lead">{guides.length} guides for installing ComfyUI, organizing models and understanding workflow errors.</p><div className="nd-actions"><Link className="nd-button" href="/guides/installation">Start with installation</Link><Link className="nd-button nd-secondary" href="/tutorials">Watch video tutorials</Link><Link className="nd-text-link" href="/workflows">Find a workflow →</Link></div><div className="nd-grid nd-section">{guides.map(g => <article key={g.slug} className="nd-card nd-card-body"><h2 style={{fontSize:'1.3rem'}}><Link href={`/guides/${g.slug}`}>{g.title}</Link></h2><p>{g.description}</p><Link className="nd-text-link" href={`/guides/${g.slug}`}>Read guide →</Link></article>)}</div></div></div>; }
